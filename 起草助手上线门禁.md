# 广告宣传合规起草助手上线门禁

## 已验证

- 本机研发版已具备医疗健康、电商促销两类文字起草。
- 链路为：客户确认事实 → 生成待审版本 → 同一审查核心复核 → 发现风险时修订并再次审查。
- 完整本机回归：28 项测试通过。
- 输出始终标为待审版本；缺少材料、未采用主张、规则编号和内部审查结果可追溯。
- 五个真实模型脱敏样例已完成：药品、保健食品、电商价格、化妆品医疗化和直播承诺均被排除或列为待补材料，未进入可发布状态。详见《起草助手脱敏验收记录》。
- 已提供 `scripts/run_draft_acceptance.py`：覆盖药品、保健食品、电商价格、化妆品医疗化、直播承诺五类脱敏样例；仅输出脱敏汇总，不保存输入原文、生成文案或密钥。

## 当前不能直接替换线上服务的原因

1. 正式服务器现有服务没有 `/api/draft`，桌面版也没有起草入口。
2. 线上依赖环境未安装起草链路需要的 `httpx`。
3. 已上线审查助手承载既有检测、历史记录和配额；整体替换会影响现有用户。
4. 服务器可用内存约 659 MiB，不应把 OCR、网页抓取等重组件与纯文字起草服务混装。

## 服务器部署验收（2026-08-09）

- 已创建独立、非特权的 `sima-draft.service`：仅监听 `127.0.0.1:8012`，单进程、`256M` 内存上限、无访问日志；systemd 安全暴露评级为 `2.9 OK`。
- 已使用独立虚拟环境和受控环境文件；密钥未写入网页、代码库或日志，规则目录仅含已核验规则。
- 回环健康检查、无缓存响应、跨域白名单、未同意条款拦截和脱敏正常起草均通过；实测内存约 `50MB`。
- 既有 `ad-checker.service`、Nginx 及 `checker.4ma.wang/api/health` 已回归正常；部署前配置已备份。
- macOS `._` 资源文件会导致规则读取失败的问题已修复并验证，部署包后续会忽略该类文件。
- 已新增 `draft.4ma.wang` A 记录并指向 `118.190.133.215`；独立 Let’s Encrypt 证书已签发，有效期至 2026-11-07，并纳入自动续期。
- 公网 HTTP→HTTPS、HTTPS 健康检查、首页 200、安全响应头、接口文档 404、无缓存及接口请求契约均已验证；完整模型链路已在同一服务器服务的回环入口验证。
- 390×844 手机布局结构验收通过：无横向溢出、表单单列、提交按钮可见；医疗健康与电商促销切换以及化妆品、直播条件字段显示正常。
- 部署中发现并修复：虚拟环境移动后解释器路径失效、macOS `._` 文件干扰规则读取、起草页复制目录错误、限流日志噪声四项问题。

## 推荐上线方式

采用独立服务、独立路径、独立回退：

1. 起草服务使用 `backend/draft_service.py` 与 `requirements-draft-service.txt`，只部署文字起草所需依赖。
2. 独立监听本机回环地址，不暴露管理接口。
3. Nginx 只为起草路径转发到新服务；旧审查服务和其 API 不改动。
4. 起草页首版只开放医疗健康和电商促销，并保留事实输入、材料提示、内部复审和待审标识。
5. 先用脱敏样例完成接口、移动端、异常状态和资源占用验收，再决定公开入口。
6. 本机 `deploy/` 已提供独立子域名、单进程限额、回环监听和 Nginx 限流模板；模板不等于已部署。

## 上线前必须满足

- 单独服务能在服务器以非特权账户运行，并设置限流、请求体上限、日志脱敏和服务自动重启。
- 模型密钥只放入服务环境变量或受控密钥存储，不写入网页、代码库或日志。
- 至少完成：正常起草、材料不足、模型失败、复审仍有风险、移动端显示五类验收。
- 运行 `scripts/run_draft_acceptance.py` 的五类真实模型验收，并保存每例的脱敏结果；模型调用无输出、无结果文件或未完成均不得算作通过。
- 保留旧服务和反向代理配置备份；上线后验证旧审查、桌面版、手机版、起草接口均正常。

## 当前结论

独立起草服务已完成正式公网上线及桌面、手机结构验收，入口为 `https://draft.4ma.wang/`。既有广告审查助手保持独立运行且回归正常。后续只进行运行监测、真实用户反馈和基于证据的规则迭代，不再扩大首版行业或加入图片审查。
