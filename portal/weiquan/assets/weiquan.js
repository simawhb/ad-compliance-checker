(() => {
  'use strict';
  const DISCLAIMER = '以上内容仅供参考，不构成法律意见，具体以有权机关认定为准。';
  const apiEndpoint = mode => mode === 'consumer' ? '/weiquan/api/consumer' : '/weiquan/api/business';
  const commonFields = ['disputeType','summary','factsKnown','factsMissing','evidenceNeeded','recommendedPath','disclaimer'];
  const consumerFields = ['legalBasis','specialNotes','letter'];
  const businessFields = ['consumerClaimBasis','businessResponsePoints','possibleAbnormalClaimFeatures','complianceCheck','replyLetter'];
  const isStringArray = value => Array.isArray(value) && value.every(item => typeof item === 'string');
  const isValidResponse = (mode, value) => {
    if (!value || typeof value !== 'object' || Array.isArray(value) || value.disclaimer !== DISCLAIMER) return false;
    const fields = commonFields.concat(mode === 'consumer' ? consumerFields : businessFields);
    if (Object.keys(value).length !== fields.length || !fields.every(field => Object.prototype.hasOwnProperty.call(value, field))) return false;
    return ['disputeType','summary',mode === 'consumer' ? 'letter' : 'replyLetter'].every(field => typeof value[field] === 'string' && value[field].trim())
      && fields.filter(field => ['factsKnown','factsMissing','evidenceNeeded','recommendedPath','legalBasis','specialNotes','consumerClaimBasis','businessResponsePoints','possibleAbnormalClaimFeatures','complianceCheck'].includes(field)).every(field => isStringArray(value[field]));
  };
  const consumerFixture = {disputeType:'商品质量与售后沟通争议（开发 Mock）',summary:'此为本地 UI 结构示例，不是实际分析结果。真实服务接入后会只依据当前提交的材料生成内容。',factsKnown:['购买的是一件日常电商商品。','用户称已发现使用异常。','用户称已联系售后。'],factsMissing:['下单和收货时间。','商品异常的具体表现和出现时间。','商家或平台的书面回复。','商品页面、售后规则和宣传承诺。'],legalBasis:['[L-001] 消费者权益保护相关规定（开发 Mock）。','[L-004] 合同履行与质量责任相关主题（开发 Mock）。'],recommendedPath:['先向商家清楚说明商品、问题、证据和希望的处理结果。','如为平台交易，可同步提交平台争议处理并保留提交记录。','协商无果时，可考虑 12315 或消费者组织等渠道。','再根据争议金额、证据和案件性质评估其他正式程序。'],evidenceNeeded:['订单','支付记录','商品页面','聊天记录','售后记录','物流记录','商品照片/视频','宣传承诺','使用说明'],specialNotes:['拍摄商品状态时保留整体、细节和使用环境。','AI 客服循环回复时，保存页面截图并记录人工转接尝试。','客观陈述事实，不夸大损失或虚构证据。'],letter:'致商家：\n\n本人就此前购买的商品与售后处理事宜，现将已知情况整理如下：商品在正常使用过程中出现异常，我已通过平台或客服渠道反馈，并保留了订单、支付、商品页面、沟通及商品状态等材料。\n\n目前我希望贵方在核对订单、商品情况和售后记录后，就退换、维修、补发或其他适当处理方式给予明确书面回复。为便于协商，请一并说明处理依据、预计完成时间及需要我补充的材料。\n\n我愿意优先通过理性协商解决争议，也将继续如实保存和补充相关材料。烦请在合理期限内回复。\n\n此致\n消费者（姓名可自行填写）',disclaimer:DISCLAIMER};
  const businessFixture = {disputeType:'事实尚待核实的商品质量与售后争议（Mock 示例）',summary:'当前材料不足以确认商品是否存在质量问题、消费者是否使用不当以及售后是否已充分履行。应先核验事实并检查自身合规问题，再作书面回应。',factsKnown:['消费者购买了多件同类商品。','投诉文本提出较高赔偿诉求。','经营者目前掌握部分订单和物流记录。','是否存在质量问题及售后处理经过尚未核实。'],factsMissing:['涉案商品批次、进货及合格证明。','消费者主张的具体问题、照片/视频及发生时间。','使用方式、保存条件和是否存在外力损坏。','客服沟通、售后方案及实际履行记录。'],consumerClaimBasis:['消费者可基于商品质量、售后承诺或宣传内容提出具体主张。','消费者证据不足不当然排除其权利，仍需结合经营者掌握的材料核验。'],businessResponsePoints:['先核对订单、商品批次、页面宣传和售后记录。','如存在自身问题，优先纠正并提出明确处理方案。','如事实有争议，以客观证据说明，避免攻击性语言。','书面回复应说明已核实事项、待补材料和下一步处理时间。'],evidenceNeeded:['进货凭证','供应商材料','产品合格证明','质检材料','商品说明','使用说明','宣传页面','售后规则','聊天记录','物流记录','签收确认','退款记录','已履行售后证明'],possibleAbnormalClaimFeatures:['购买数量较多。','投诉表达具有模板化特征。','在充分协商前提出较高赔偿要求。'],complianceCheck:['商品质量','标签标识','宣传表述','售后承诺','退换货规则','价格标示'],recommendedPath:['立即保全订单、页面、物流和沟通记录。','核实商品质量、宣传和售后是否存在真实问题。','向消费者发送克制的书面回复，说明已核实事实与待补材料。','如涉及宣传表述，使用既有广告审查工具复核，不自行下结论。'],replyLetter:'致消费者：\n\n已收到你关于相关订单的反馈。我们正在核对订单信息、商品批次、页面说明、物流签收及既有沟通记录，以便准确了解实际情况。\n\n目前，部分事实和材料仍需进一步核实。若你方便，请补充能够说明商品状态、问题出现时间及使用情况的材料；我们也将同步核对内部进货、质量、售后和宣传资料。\n\n如核实发现商品、宣传或售后环节存在应当改进或处理的问题，我们会依法依规提出相应方案；如双方对事实存在分歧，也会基于已核实材料继续沟通。请避免提交与争议无关的敏感个人信息。\n\n感谢你的反馈，我们将于合理期限内再次回复。\n\n经营者（名称可自行填写）',disclaimer:DISCLAIMER};
  const esc = value => String(value).replace(/[&<>\"]/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[char]));
  const array = value => Array.isArray(value) ? value : [];
  const list = items => `<ul>${array(items).map(item => `<li>${esc(item)}</li>`).join('')}</ul>`;
  const checks = items => `<div class="checklist">${array(items).map((item,index) => `<label><input type="checkbox" data-evidence="${index}"><span>${esc(item)}</span></label>`).join('')}</div>`;
  const section = (title, body, full = false) => `<section class="result-section${full ? ' full':''}"><h3>${esc(title)}</h3>${body}</section>`;
  const render = (mode, data, isMock) => {
    const root = document.getElementById('result'); const isConsumer = mode === 'consumer';
    const sections = [section(isConsumer ? '纠纷类型' : '争议初步分类', `<p>${esc(data.disputeType)}</p>`),section('情况判断', `<p>${esc(data.summary)}</p>`),section('当前可确认事实', list(data.factsKnown)),section('仍需补充的事实', list(data.factsMissing)),section(isConsumer ? '主要法律依据' : '消费者主张依据', list(isConsumer ? data.legalBasis : data.consumerClaimBasis)),section(isConsumer ? '建议处理路径' : '经营者可说明事项', isConsumer ? `<ol class="path-list">${array(data.recommendedPath).map(item => `<li>${esc(item)}</li>`).join('')}</ol>` : list(data.businessResponsePoints)),section(isConsumer ? '证据清单（仅当前页面）' : '证据与反证清单（仅当前页面）', checks(data.evidenceNeeded), true)];
    if (isConsumer) sections.push(section('专项提醒', list(data.specialNotes), true));
    else { sections.push(section('需进一步核实的异常特征', list(data.possibleAbnormalClaimFeatures), true)); sections.push('<section class="result-section full"><h3>重要说明</h3><div class="guardrail">上述特征仅供进一步核实，不足以据此认定投诉人为职业打假人、恶意投诉人，也不能据此直接否定其消费者权利或具体主张。</div></section>'); sections.push(section('自身合规检查', `<div class="compliance">${array(data.complianceCheck).map(item => `<span>${esc(item)}</span>`).join('')}</div>`, true)); sections.push(section('建议处理路径', `<ol class="path-list">${array(data.recommendedPath).map(item => `<li>${esc(item)}</li>`).join('')}</ol>`, true)); }
    const letter = isConsumer ? data.letter : data.replyLetter;
    sections.push(section(isConsumer ? `正式协商函${isMock ? '（开发 Mock）' : ''}` : `正式回复函${isMock ? '（开发 Mock）' : ''}`, `<div class="letter">${esc(letter)}</div><div class="actions"><button class="button" type="button" id="copy-letter">复制${isConsumer ? '协商函' : '回复函'}</button></div>`, true));
    if (!isConsumer) sections.push('<section class="result-section full"><a class="tool-link" href="https://checker.4ma.wang/" target="_blank" rel="noopener noreferrer">检查广告宣传合规 <span aria-hidden="true">↗</span></a><p>仅在争议涉及商品页面、直播或其他宣传表述时使用该既有工具。</p></section>');
    root.innerHTML = `<div class="result-top"><h2 class="result-title">分析结果</h2><span class="mock-label">${isMock ? '开发 Mock 数据，非真实 AI' : '基于当前提交内容生成，请自行核验'}</span></div><div class="result-grid">${sections.join('')}</div><div class="disclaimer">${esc(data.disclaimer || DISCLAIMER)}</div>`;
    root.hidden = false; document.getElementById('copy-letter').addEventListener('click', () => copyLetter(letter));
  };
  const copyLetter = async text => { const status = document.getElementById('state-message'); try { if (navigator.clipboard && window.isSecureContext) await navigator.clipboard.writeText(text); else { const input = document.createElement('textarea'); input.value = text; input.style.position = 'fixed'; input.style.opacity = '0'; document.body.appendChild(input); input.select(); document.execCommand('copy'); input.remove(); } status.className = 'state-message ok'; status.textContent = '已复制到剪贴板。'; } catch { status.className = 'state-message error'; status.textContent = '暂时无法复制，请手动选择文本复制。'; } };
  const init = () => {
    const mode = document.body.dataset.mode; if (!mode) return;
    const input = document.getElementById('case-text'), analyze = document.getElementById('analyze'), reset = document.getElementById('reset'), message = document.getElementById('state-message'), result = document.getElementById('result');
    const emptyText = '请描述争议经过；请勿输入与争议无关的敏感信息。'; let mockTimer = null; let controller = null;
    const set = (name, text, kind = '') => { message.dataset.state = name; message.className = `state-message ${kind}`; message.textContent = text; };
    const showFailure = status => { result.hidden = true; if (status === 429) set('provider-unavailable', '当前请求较多，请稍后再试。', 'error'); else if (status === 502 || status === 503 || status === 504) set('provider-unavailable', '智能分析服务当前不可用。你的输入不会因此建立案件记录。', 'error'); else set('error', '暂时无法完成分析，请稍后重试。', 'error'); };
    const analyzeRemote = async (text, analysisMode) => { analyze.disabled = true; controller = new AbortController(); const timeout = window.setTimeout(() => controller.abort(), 70000); set('loading', '正在分析你提交的文字；请勿关闭此页面。'); try { const response = await fetch(apiEndpoint(analysisMode), {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text}),cache:'no-store',credentials:'same-origin',signal:controller.signal}); const payload = await response.json().catch(() => null); if (!response.ok || !isValidResponse(analysisMode, payload)) { showFailure(response.ok ? 502 : response.status); return; } render(analysisMode, payload, false); set('success', '已生成参考信息。请以事实和受控法律来源为准。', 'ok'); } catch (error) { showFailure(error && error.name === 'AbortError' ? 504 : 0); } finally { window.clearTimeout(timeout); controller = null; analyze.disabled = false; } };
    input.addEventListener('input', () => input.value.trim() ? set('typing', '正在整理你的描述；提交前请删除无关敏感信息。') : set('empty', emptyText));
    analyze.addEventListener('click', () => { const text = input.value.trim(); if (text.length < 12) { set('validation', '请补充商品/服务、争议经过和希望解决的问题。', 'error'); input.focus(); return; } analyzeRemote(text, mode); });
    reset.addEventListener('click', () => { window.clearTimeout(mockTimer); if (controller) controller.abort(); input.value = ''; result.hidden = true; result.innerHTML = ''; analyze.disabled = false; set('empty', emptyText); input.focus(); });
    // Hidden developer-only helper: no URL data and no persistent storage.
    window.__weiquanDebug = { showMock() { render(mode, mode === 'consumer' ? consumerFixture : businessFixture, true); set('success', '开发 Mock 结果已显示。', 'ok'); }, showError() { showFailure(0); }, showProviderUnavailable() { showFailure(503); }, isValidResponse };
  };
  document.addEventListener('DOMContentLoaded', init);
})();
